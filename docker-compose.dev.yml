version: "3.9"

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mydb
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app-api:
    build: .
    restart: always
    depends_on:
      - db
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    ports:
      - "8005:8000"
    volumes:
      - .:/app   # код меняем на лету

  app-worker:
    build: .
    restart: always
    depends_on:
      - db
    command: python worker.py
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app   # чтобы видеть изменения в reports.py

  adminer:
    image: adminer
    restart: always
    ports:
      - "8085:8080"
    depends_on:
      - db

volumes:
  postgres_data: